import { makeStyles } from '@mui/styles'

export const useStyle = makeStyles({
    title: {
        fontSize: 20,
        fontWeight: 'bold',
    },
    name: {
        paddingLeft: 5,
        display: 'block',
        fontSize: 14,
        fontWeight: 'bold',
        color: 'black',
    },
})
