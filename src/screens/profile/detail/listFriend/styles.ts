import {makeStyles} from '@mui/styles'

export const useStyle = makeStyles({
  title: {
    fontSize: 20,
    fontWeight: 'bold',
  },
  frame: {
    marginBottom: 10,
  },
  avatar: {
    width: 100,
    height: 100,
    borderRadius: 8,
  },
  name: {
    fontSize: 14,
    fontWeight: 'bold',
    marginLeft: 10,
    color: 'black',
  },
})
