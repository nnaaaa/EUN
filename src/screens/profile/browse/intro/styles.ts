import { makeStyles } from '@mui/styles'

export const useStyle = makeStyles({
    title: {
        fontSize: 20,
        fontWeight: 'bold',
    },
    link: {
        fontSize: 14,
    },
    hobbies: {
        marginRight: 10,
        marginBottom: 10,
    },
    education: {
        textTransform: 'none',
        width: '100%',
    },
})
