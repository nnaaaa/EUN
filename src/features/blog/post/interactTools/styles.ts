import { makeStyles } from '@mui/styles'

export const useStyle = makeStyles({
    button: {
        flex: 1,
        justifyContent: 'center',
        overflow: 'hidden',
    },
    textBtn: {
        textAlign: 'center',
        whiteSpace: 'nowrap',
        overflow: 'hidden',
    },
})
